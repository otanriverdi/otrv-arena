# otrv-arena

Minimal single-file arena allocator compatible with C++20. Also includes an STL compatible allocator.

## Usage

You need one file that defines the implementation macro.

```c
#define OTRV_ARENA_IMPLEMENTATION
#include "otrv_arena.h"
```
